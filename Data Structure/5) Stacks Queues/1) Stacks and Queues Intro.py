# Stacks ==> LIFO only can grap last item O(1) but lookup is O(n)
# LIFO used when undo
# Arrays/LinkedLists

# Queues ==> FIFO enqueue,O(1) // dequeue,O(1)
# LIFO used when provide service for customer (Uber ride... any other queue)
# LIFO used also with (Printers)
# LinkedLists only as Array's prepending is O(n)